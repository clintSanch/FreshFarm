import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.freshfarm.app',
  appName: 'FreshFarm',
  webDir: 'dist/HybridFreshFarm/browser',
  bundledWebRuntime: false
};

export default config;
